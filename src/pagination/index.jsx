import React from "react";

function Pagination({
  currentPage,
  setCurrentPage,
  rowStart,
  setRowStart,
  pageSize,
  setPageSize,
  rowEnd,
  setRowEnd,
}) {
  return <div></div>;
}

export default Pagination;
